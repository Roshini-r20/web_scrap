services:
  - type: web
    name: puppeteer-scraper-api
    env: node
    buildCommand: |
      apt-get update && \
      apt-get install -y chromium-browser && \
      npm install && \
      which chromium-browser > .chrome-path
    startCommand: |
      export CHROME_PATH=$(cat .chrome-path) && \
      npm start
